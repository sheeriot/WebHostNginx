access_log /var/log/nginx/access.log main_ext;
access_log /dev/stdout main_ext;

# Note: WebSocket-specific logging is configured in the /ws/ location block
# using the websocket_ext format, logging to both /var/log/nginx/websocket.log
# and /dev/stdout for Docker container logs visibility
