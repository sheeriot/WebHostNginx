services:
  nginx:
    environment:
      CERTBOT_DOMAINS: "${CERTBOT_FQDN:-.}"
      CERTBOT_EMAIL: "${CERTBOT_EMAIL:-.}"
      NGINX_FQDN: "${NGINX_FQDN:-.}"
    build: .
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
      - intaker_intaker_staticfiles:/var/www/intaker/static
    # networks:
    #   - webhost

  # certbot:
  #   image: certbot/certbot
  #   volumes:
  #     - ./data/certbot/conf:/etc/letsencrypt
  #     - ./data/certbot/www:/var/www/certbot

volumes:
  intaker_intaker_staticfiles:
    external: true

networks:
  default: 
    name: webhost
    external: true